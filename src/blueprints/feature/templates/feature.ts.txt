import {Feature} from 'logic/store';
import {reducer} from 'logic/__feature_dir__/reducer';
import {NgModule} from '@angular/core';
import {ActionReducer} from '@ngrx/store';
import {EffectsModule} from '@ngrx/effects';
import {__feature_class_name__Effects} from 'logic/__feature_dir__/effects';

const featureName = '__feature_dir__';

@NgModule({
  imports: [
    EffectsModule.run(__feature_class_name__Effects)
  ],
  providers: [
    {provide: Feature, useClass: __feature_class_name__Feature, multi: true},
  ]
})
export class __feature_class_name__Feature implements Feature {

  static select(store: any) {
    return store[featureName];
  }

  getNamespace(): string {
    return featureName;
  }

  createActionReducer(): ActionReducer<any> {
    return reducer;
  }
}

export * from './actions';
